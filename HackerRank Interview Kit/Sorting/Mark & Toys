# Complete the maximumToys function below.
def maximumToys(prices, k):
    prices.sort()
    count=0
    total=0

    for i in range(len(prices)):
        total+=prices[i]
        if total<=k:
            count+=1
    print(count) 
    return (count)

